<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    <link rel="stylesheet" href="{{ 'tailwind.css' | asset_url }}">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/collections/all">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
            </ul>
        </nav>
    </header>
    <main>
        {{ content_for_layout }}
    </main>
    <footer>
        <p>&copy; {{ 'now' | date: '%Y' }} BottlePop</p>
    </footer>
    <script src="{{ 'alpine.js' | asset_url }}"></script>
</body>
</html>